CREATE TABLE tenant (
	tenant_id serial PRIMARY KEY,
	tenant_name VARCHAR ( 100 ) UNIQUE NOT NULL,
	tenant_details VARCHAR (1024) NOT NULL,
	created_on TIMESTAMP NOT NULL
);

CREATE TABLE users(
	username VARCHAR (50) PRIMARY KEY,
	password VARCHAR (50) NOT NULL,
	tenant_id INT NOT NULL,
	created_on TIMESTAMP NOT NULL
);

CREATE TABLE product(
	product_id serial PRIMARY KEY,
	name VARCHAR (100) NOT NULL,
	tenant_id INT NOT NULL,
	price DECIMal NOT null
);

CREATE TABLE "order"(
	order_id serial PRIMARY KEY,
	user_id INT NOT NULL,
	datetime Timestamp NOT null
);

INSERT INTO public.tenant
(tenant_name, tenant_details, created_on)
VALUES('Amazon', 'Shop on the Amazon App. Fast, convenient and secure.Over 17 crore products in your pocket', current_timestamp);

INSERT INTO public.tenant
(tenant_name, tenant_details, created_on)
VALUES('Flipkart', 'Flipkart: The One-stop Shopping Destination. E-commerce is revolutionizing the way we all shop in India.', current_timestamp);

INSERT INTO "users" VALUES 
  ('kmarke','abc123','1', Current_TimeStamp),
  ('asinha','abc123','2', Current_TimeStamp),
  ('mkotian','abc123','2', Current_TimeStamp);

CREATE TABLE order_product (
	id serial PRIMARY KEY,
	order_id INT NOT NULL,
	product_id INT NOT NULL,
	product_quantity INT NOT null
);

INSERT INTO public.product values(1,'PeterEnglandShirt', 1, 100),
(5,'PeterEnglandShirt', 2, 100),
(2,'X TShirt', 1, 20),
(3,'Woodland Jackets', 2, 56),
(4,'T-Shirt', 1, 48);

INSERT INTO public."order"
(user_id, datetime)
VALUES(1, current_timestamp),
(2, current_timestamp),
(1, current_timestamp),
(2, current_timestamp);

INSERT INTO public.order_product
(order_id, product_id, product_quantity)
VALUES(1, 1, 1);